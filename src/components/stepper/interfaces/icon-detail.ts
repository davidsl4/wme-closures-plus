export interface IconDetail {
  /** The class name for the icon font, e.g., "icon-user" */
  name: string;

  /** The desired color, e.g., "blue", "#FF5733", "var(--some-css-variable)" */
  color?: string;
}
